def call_llm(*, system_prompt: str, user_prompt: str) -> str:
    """
    Placeholder service.
    En tests, cette fonction est mockée.
    """
    raise RuntimeError("LLM backend not configured yet.")
from django.apps import AppConfig


class ShellePremiumConfig(AppConfig):
    """Configuration de l'application Shelle Premium."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "shelle_premium"
    verbose_name = "Shelle Premium"

